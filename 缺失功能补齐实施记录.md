# 缺失功能补齐实施记录

生成日期：2026-06-22

## 已补齐功能

本次在现有 JSP + Servlet + JDBC + MySQL 架构内补齐了以下功能，没有引入新框架。

### 1. 系统开放状态开关

新增能力：

- 管理员维护系统开关。
- 教师出题开关关闭后，教师不能新增或修改课题。
- 学生选题开关关闭后，学生不能提交选题申请。
- 开题报告、中期报告、终稿上传开关关闭后，学生不能提交对应阶段文档。

核心文件：

- `src/util/SystemSwitchUtil.java`
- `src/controller/AdminSystemSwitchController.java`
- `WebContent/admin/system-switches.jsp`
- `src/controller/TeacherTopicController.java`
- `src/controller/StudentTopicController.java`
- `src/controller/StudentDocumentController.java`

核心路由：

- `/admin/system-switch.action`

### 2. 课题审核流程

新增能力：

- 教师新增或编辑课题后，课题进入 `pending` 待审核状态。
- 管理员在课题审核页面通过或驳回课题。
- 审核通过后课题进入 `open`，学生才能看到并申请。
- 驳回后课题进入 `rejected`，教师可修改后重新提交。
- 记录审核意见、审核人、审核时间。

核心文件：

- `src/controller/AdminTopicReviewController.java`
- `WebContent/admin/topic-review.jsp`
- `src/dao/TopicDao.java`
- `src/bean/Topic.java`
- `src/controller/TeacherTopicController.java`
- `src/controller/StudentTopicController.java`

核心路由：

- `/admin/topic-review.action`

### 3. 教师/学生 Excel 批量导入

新增能力：

- 管理员可导入教师账号。
- 管理员可导入学生账号。
- 学生用户名可留空，默认使用学号。
- 密码可留空，默认 `123456`。
- 导入时做用户名、学号重复校验。
- 页面展示成功/跳过数量和部分跳过原因。

Excel 列顺序：

1. 用户名
2. 姓名
3. 学号
4. 学院代码
5. 专业代码
6. 班级
7. 部门/院系
8. 邮箱
9. 电话
10. 初始密码

核心文件：

- `src/controller/AdminUserImportController.java`
- `src/controller/AdminUserController.java`
- `src/dao/UserDao.java`
- `src/bean/UserSearchCriteria.java`
- `WebContent/admin/users.jsp`

核心路由：

- `/admin/user-import.action`

### 4. 管理员多条件用户查询

新增能力：

- 按角色查询。
- 按学院查询。
- 按专业查询。
- 按班级模糊查询。
- 按学号模糊查询。
- 按姓名模糊查询。
- 支持组合条件分页查询。

核心文件：

- `src/bean/UserSearchCriteria.java`
- `src/dao/UserDao.java`
- `src/controller/AdminUserController.java`
- `WebContent/admin/users.jsp`

核心路由：

- `/admin/user.action`

### 5. 学生密码批量重置

新增能力：

- 管理员可勾选学生账号后批量重置密码。
- 管理员可按当前筛选条件批量重置学生密码。
- 后端限制只重置 `role='student'` 的账号，教师和管理员不会被误重置。

核心文件：

- `src/controller/AdminUserController.java`
- `src/dao/UserDao.java`
- `WebContent/admin/users.jsp`

### 6. 文件模板管理

新增能力：

- 管理员上传文件模板。
- 管理员删除模板。
- 教师下载模板。
- 学生下载模板。
- 模板可按文档阶段筛选。

核心文件：

- `src/bean/FileTemplate.java`
- `src/dao/FileTemplateDao.java`
- `src/controller/FileTemplateController.java`
- `src/controller/FileTemplateDownloadController.java`
- `WebContent/admin/file-templates.jsp`
- `WebContent/teacher/file-templates.jsp`
- `WebContent/student/file-templates.jsp`

核心路由：

- `/admin/file-template.action`
- `/teacher/file-template.action`
- `/student/file-template.action`
- `/file-template-download.action`

### 7. 个人中心与自助修改密码

新增能力：

- 三类用户均可进入个人中心。
- 用户可查看自己的账号信息。
- 用户可修改邮箱、电话。
- 用户可修改密码，修改时校验旧密码。

核心文件：

- `src/controller/ProfileController.java`
- `WebContent/profile.jsp`
- `src/dao/UserDao.java`
- `WebContent/WEB-INF/includes/sidebar.jsp`

核心路由：

- `/profile.action`

### 8. 系主任角色与专业维度管理

新增能力：

- 新增 `director` 系主任角色。
- 系主任账号绑定 `college + major`，即绑定到具体学院/学部下的具体专业。
- 系主任采用 RBAC 角色层级 / 角色继承模型：`director = teacher + director-extra`。
- 系主任登录后拥有教师端全部功能，并额外拥有本专业管理菜单：
  - 本专业课题审核
  - 本专业项目统计与导出
- 系主任审核课题时只能查看、通过、驳回自己专业范围内的课题。
- 系主任统计与导出按自己专业过滤，不再导出全校数据。
- 公告管理、系统开关、用户管理、全校统计导出仍属于管理员权限，系主任不继承管理员权限。
- 教师出题增加课题所属专业，专业下拉从数据库 `majors` 表加载。
- 学生浏览课题支持学院/专业联动筛选，默认按学生本人学院和专业展示。
- 学生提交选题时后端校验课题与学生是否属于同一学院/专业，防止绕过前端跨专业选题。

组织口径：

- 保留多学院/学部，不把全校压成一个学院：
  - 人工智能学部
  - 经管学院
  - 文科学部
  - 电气工程学部
  - 能源与机械动力学院
  - 环境与化学工程学院
- 原“计算机学院”“软件学院”历史数据归并到“人工智能学部”。
- 人工智能学部有效专业：
  - 计算机科学与技术
  - 软件工程
  - 数据科学与大数据技术
  - 人工智能

核心文件：

- `src/bean/Topic.java`
- `src/dao/TopicDao.java`
- `src/dao/StatsDao.java`
- `src/dao/SelectionDao.java`
- `src/controller/AdminTopicReviewController.java`
- `src/controller/AdminStatsController.java`
- `src/controller/AdminExportController.java`
- `src/controller/TeacherTopicController.java`
- `src/util/RoleUtil.java`
- `src/filter/AuthFilter.java`
- `src/controller/FileTemplateController.java`
- `src/controller/AiAssistantController.java`
- `src/controller/MessageController.java`
- `src/controller/DownloadController.java`
- `src/controller/StudentTopicController.java`
- `WebContent/teacher/topics.jsp`
- `WebContent/student/topics.jsp`
- `WebContent/admin/topic-review.jsp`
- `WebContent/dashboard.jsp`
- `WebContent/director/statistics.jsp`

核心路由：

- `/director/topic-review.action`
- `/director/statistics.jsp`
- `/director/stats.action`
- `/director/export.action`
- `/teacher/topic.action`
- `/teacher/selection.action`
- `/teacher/document.action`
- `/teacher/students.jsp`
- `/teacher/defense.jsp`
- `/teacher/file-template.action`
- `/teacher/ai.jsp`
- `/teacher/messages.jsp`

关键账号，密码均为 `123456`：

- `director_ai_cs`：人工智能学部 / 计算机科学与技术
- `director_ai_se`：人工智能学部 / 软件工程
- `director_ai_ds`：人工智能学部 / 数据科学与大数据技术
- `director_ai_ai`：人工智能学部 / 人工智能

## 数据库变更

新增或更新的 SQL：

- `sql/migrate_missing_features_20260622.sql`
- `sql/migrate_file_templates_profile_20260622.sql`
- `sql/migrate_director_role_20260622.sql`
- `sql/migrate_ai_faculty_major_scope_20260622.sql`
- `sql/seed_demo_data_20260622.sql`
- `sql/init.sql`

关键变更：

- `topics` 增加审核字段：
  - `review_comment`
  - `reviewer_id`
  - `review_time`
- `topics.status` 扩展为：
  - `pending`
  - `open`
  - `closed`
  - `rejected`
- 新增 `file_templates` 表。
- `users.role` 增加 `director`。
- `topics` 增加 `major` 字段，课题可按学院/专业归属。
- `colleges`、`majors` 字典改为数据库维护，前端不硬编码组织数据。
- 旧学院/专业口径迁移：
  - `cs`、`sw` 归并为 `ai`
  - `cy`、`sw`、`se` 归并为 `se`
  - `bigdata`、`ds` 归并为 `ds`
- 新增系统开关配置：
  - `switch.topic_submit`
  - `switch.selection`
  - `switch.selection_round1`
  - `switch.selection_round2`
  - `switch.upload_proposal`
  - `switch.upload_midterm`
  - `switch.upload_final`

## 已执行验证

已执行：

```powershell
mvn test
mvn package -DskipTests
mysql -u root -p12345 --default-character-set=utf8mb4 graduation_design -e "source sql/migrate_director_role_20260622.sql"
mysql -u root -p12345 --default-character-set=utf8mb4 graduation_design -e "source sql/migrate_ai_faculty_major_scope_20260622.sql"
mysql -u root -p12345 --default-character-set=utf8mb4 graduation_design -e "source sql/seed_demo_data_20260622.sql"
```

结果：

- Java 编译通过。
- JUnit 测试通过。
- WAR 打包成功。

已启动本地服务并冒烟验证：

- `http://localhost:8086/graduation-design/dashboard.jsp`
- `/admin/user.action`
- `/admin/topic-review.action`
- `/admin/system-switch.action`
- `/admin/file-template.action`
- `/profile.action`
- `/teacher/topic.action`
- `/teacher/file-template.action`
- `/student/topic.action`
- `/student/document.action`
- `/student/file-template.action`
- `/director/topic-review.action`
- `/director/statistics.jsp`
- `/director/stats.action`
- `/director/export.action`

上述页面均返回 HTTP 200。

额外验证：

- `director_ai_cs` 只能看到“计算机科学与技术”课题，不能看到“人工智能”“软件工程”课题。
- `director_ai_ai` 只能看到“人工智能”课题。
- `director_ai_se` 只能看到“软件工程”课题。
- 管理员用户页的学院/专业筛选项来自数据库，旧“计算机学院”不再作为有效学院显示。
- 教师课题页和学生选题页的专业下拉来自数据库，旧“机器学习/计算机视觉/NLP”等演示专业不再作为人工智能学部有效专业显示。
- 数据库检查：
  - `invalid_topic_major = 0`
  - `invalid_user_major = 0`
  - `active_old_colleges = 0`

## 仍未完全补齐的功能

以下功能本次没有完全展开：

1. 真正的一轮/二轮选题状态机。
   - 当前实现了统一选题开关，并保留了 `switch.selection_round1`、`switch.selection_round2` 配置。
2. 教师分组管理。
3. 手动分配未选题学生。
4. 完整评分体系。
   - 当前仍是阶段文档分数 + 答辩分数，未新增自评/互评/综合成绩表。

这些功能如果继续做，建议下一步优先补：

1. `selection_rounds` 表和一轮/二轮选题状态。
2. 未选学生手动分配。
3. 教师分组表和分组页面。
4. 综合评分表。

## 2026-06-22 系主任模块重构记录

本次按“系主任模块独立化、权限按专业收口、跨专业数据清理”的重构计划执行。

### 1. 模块拆分

- 课题审核不再由 `AdminTopicReviewController` 同时处理 `/admin/*` 与 `/director/*`。
  - 管理员：`src/controller/AdminTopicReviewController.java`
  - 系主任：`src/controller/DirectorTopicReviewController.java`
- 统计 API 拆分：
  - 管理员：`/admin/stats.action`
  - 系主任：`/director/stats.action`
- 成绩导出拆分：
  - 管理员导出全校成绩：`/admin/export.action`
  - 系主任导出本专业成绩：`/director/export.action`
- 系统开关和公告管理回收给管理员，不再放入系主任模块。
- 2026-06-23 按用户确认补齐角色继承：系主任不是管理员分支，而是教师上一级，继承教师端全部能力，再叠加本专业课题审核、统计、导出。

### 2. 独立系主任页面

新增或替换为真正的 `/director/*` 页面，不再通过 JSP forward 复用 `/admin/*` 页面：

- `WebContent/director/topic-review.jsp`
- `WebContent/director/statistics.jsp`

### 3. 权限与作用域收口

- 新增 `bean.UserScope` 和 `util.ScopeUtil`。
- 系主任必须绑定 `college + major`，否则后端返回 403，不再回退到全局数据。
- 系主任所有关键查询按本人 `college + major` 强制过滤。
- `util.RoleUtil` 定义 `director` 对 `teacher` 的继承关系；`AuthFilter` 允许系主任访问 `/teacher/*`，但 `/admin/*` 仍只允许管理员。
- 教师继承能力仍按本人 `user.id` 限定，例如“我的课题、选题审批、文档审核、学生进度、答辩安排”只看本人作为指导教师负责的数据；系主任额外管理能力才按 `college + major` 扩展到本专业。
- 文件模板、AI、站内消息、附件下载已处理原来只认 `teacher` 字符串导致的系主任访问问题。
- 学生浏览课题不再接收 URL 中的 `college`、`major` 参数，始终只显示本人学院/专业课题。
- 学生提交选题、教师出题、学生文档上传仍按管理员维护的全局系统开关判断。

### 4. 数据库变更

- `announcements` 增加：
  - `scope_type`
  - `college`
  - `major`
- 新增迁移脚本：
  - `sql/migrate_director_scope_refactor_20260622.sql`
  - `sql/migrate_director_admin_boundary_20260623.sql`
- 迁移脚本会将历史跨专业有效选题置为 `rejected`，并重新计算 `topics.selected_count`。
  2026-06-23 边界修正脚本会删除专业级系统开关覆盖表，公告和系统开关回收为管理员权限。

### 5. 本次重构验证

已执行：

```powershell
mvn test
mvn -DskipTests package
mysql -uroot -p12345 --default-character-set=utf8mb4 graduation_design -e "source sql/migrate_director_scope_refactor_20260622.sql"
```

数据库验证结果：

- `director_without_scope = 0`
- `active_cross_major_selection = 0`

HTTP 冒烟验证：

- `director_ai_cs` 登录后访问：
  - `/dashboard.jsp` -> 200
  - `/director/topic-review.action` -> 200
  - `/director/statistics.jsp` -> 200
  - `/director/stats.action` -> 200
  - `/admin/statistics.jsp` -> 302，返回仪表盘
- `admin` 登录后访问：
  - `/admin/topic-review.action` -> 200
  - `/admin/statistics.jsp` -> 200
  - `/admin/system-switch.action` -> 200
  - `/admin/announcements.jsp` -> 200
  - `/director/statistics.jsp` -> 302，返回仪表盘
- `student01` 访问 `/student/topic.action?college=ee&major=ee` -> 200，但页面仍只显示本人 `ai/cs` 专业课题。

作用域验证：

- `director_ai_cs` 课题审核页包含“计算机科学与技术”课题。
- `director_ai_cs` 课题审核页不包含“电气工程”“软件工程”课题。
- `/director/stats.action` 返回本专业统计 JSON。
- `/director/system-switch.action`、`/director/announcements.jsp` 已从系主任权限中撤回；系统开关和公告管理仅保留在管理员端。
