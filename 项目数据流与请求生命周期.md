# 项目数据流与请求生命周期

## 1. HTTP 请求总流程

```mermaid
flowchart TD
    A[浏览器请求] --> B[Tomcat]
    B --> C[AuthFilter]
    C --> D{公开资源?}
    D -- 是 --> E[JSP/Servlet/静态资源]
    D -- 否 --> F{已登录且角色正确?}
    F -- 否 --> G[重定向登录页或首页]
    F -- 是 --> H{非安全请求?}
    H -- 是 --> I[校验 CSRF]
    H -- 否 --> J[Controller]
    I --> J
    J --> K[DAO]
    K --> L[SQLHelper]
    L --> M[(MySQL)]
    M --> K
    K --> J
    J --> N{查询还是写操作?}
    N -- 查询 --> O[request.setAttribute]
    O --> P[forward JSP]
    N -- 写操作 --> Q[redirect 到 action]
    P --> R[HTML Response]
    Q --> A
```

## 2. 登录数据流

```mermaid
sequenceDiagram
    participant B as Browser
    participant L as login.jsp
    participant C as LoginController
    participant U as UserDao
    participant P as PasswordUtil
    participant H as SQLHelper
    participant D as MySQL
    participant S as HttpSession

    B->>L: GET /login.jsp
    L-->>B: 登录表单
    B->>C: POST /login.action
    C->>C: 读取 username/password
    C->>U: validate()
    U->>H: SELECT users WHERE username=?
    H->>D: PreparedStatement
    D-->>H: 用户记录
    H-->>U: User
    U->>P: matches(password, hash)
    P-->>U: true/false
    alt 登录成功
        C->>S: setAttribute(loginUser)
        C-->>B: redirect dashboard.jsp
    else 登录失败
        C-->>B: redirect login.jsp?error=1
    end
```

关键知识点：

- 表单 POST
- request 参数
- DAO 查询
- PreparedStatement
- Session
- response redirect

## 3. 学生浏览课题数据流

入口：

```text
GET /student/topic.action
```

```mermaid
sequenceDiagram
    participant B as Browser
    participant F as AuthFilter
    participant C as StudentTopicController
    participant D as TopicDao
    participant H as SQLHelper
    participant M as MySQL
    participant J as topics.jsp

    B->>F: GET topic.action?keyword=...
    F->>C: 学生身份通过
    C->>C: 获取 keyword/college
    C->>D: findOpenTopics()
    D->>H: queryList()
    H->>M: SELECT + JOIN
    M-->>H: rows
    H-->>D: List<Object[]>
    D-->>C: List<Topic>
    C->>C: setAttribute("topics")
    C->>J: forward
    J-->>B: 课题 HTML 表格
```

数据对象变化：

```text
数据库记录
  ↓
Object[]
  ↓
Topic JavaBean
  ↓
List<Topic>
  ↓
request 属性
  ↓
JSP 表格行
```

## 4. 学生申请课题数据流

```mermaid
sequenceDiagram
    participant B as Browser
    participant F as AuthFilter
    participant C as StudentTopicController
    participant D as SelectionDao
    participant M as MySQL
    participant L as OperationLogUtil

    B->>F: POST topic.action
    F->>F: Session + role + CSRF
    F->>C: 放行
    C->>D: apply(studentId, topicId, reason)
    D->>M: BEGIN
    D->>M: 锁定学生
    D->>M: 检查有效申请
    D->>M: 锁定课题
    D->>M: 检查状态和名额
    D->>M: INSERT selection
    D->>M: COMMIT
    D-->>C: 申请 ID / 错误码
    C->>L: 记录操作
    C-->>B: redirect my-selection.jsp
```

为什么要事务：

```text
检查是否已有选题
+ 检查课题名额
+ 插入申请
```

必须作为一个整体，否则两个并发请求可能同时通过检查。

## 5. 教师审批选题数据流

### 5.1 查询页面

```text
GET /teacher/selection.action?status=pending
```

Controller 查询后：

```java
request.setAttribute("selections", selections);
request.setAttribute("statusFilter", status);
forward("/teacher/selections.jsp");
```

JSP 只负责：

- 遍历 `List<TopicSelection>`
- 输出学生、课题和状态
- 生成批准/驳回表单

### 5.2 审批写操作

```mermaid
flowchart TD
    A[教师提交批准] --> B[AuthFilter]
    B --> C[TeacherSelectionController]
    C --> D[SelectionDao.review]
    D --> E[开启事务]
    E --> F[验证申请属于当前教师]
    F --> G[锁定学生和课题]
    G --> H{学生已有通过课题?}
    H -- 是 --> I[回滚]
    H -- 否 --> J{课题开放且有名额?}
    J -- 否 --> I
    J -- 是 --> K[申请改为 approved]
    K --> L[selected_count + 1]
    L --> M{达到上限?}
    M -- 是 --> N[课题改为 closed]
    M -- 否 --> O[保持 open]
    N --> P[提交事务]
    O --> P
    P --> Q[发送通知并写日志]
    Q --> R[redirect selection.action]
```

## 6. 学生文档提交数据流

### 6.1 页面加载

```text
GET /student/document.action?type=proposal
```

Controller 准备：

- `approvedSelection`
- `currentDocument`
- `documentVersions`
- `activeType`
- `typeNames`

然后 forward 到 `student/documents.jsp`。

这体现课程 MVC 的关键原则：

> JSP 接收数据并展示，不在页面中创建 SelectionDao、DocumentDao 和
> DocumentVersionDao。

### 6.2 上传和提交

```mermaid
sequenceDiagram
    participant B as Browser
    participant C as StudentDocumentController
    participant F as FileUploadUtil
    participant D as DocumentDao
    participant M as MySQL

    B->>C: multipart POST document.action
    C->>C: 检查 approved 选题
    C->>C: 检查 docType
    C->>D: isStageAvailable()
    D->>M: 查询前置文档状态
    M-->>D: reviewed / not reviewed
    C->>F: saveFile(Part)
    F->>F: 大小、扩展名、UUID
    F-->>C: filePath
    C->>D: submit(Document)
    D->>M: BEGIN + 锁定数据
    D->>M: 保存旧版本
    D->>M: INSERT/UPDATE document
    D->>M: COMMIT
    D-->>C: result
    alt 成功
        C-->>B: redirect document.action?msg=submit_ok
    else 失败
        C->>C: 删除本次新文件
        C-->>B: redirect error message
    end
```

## 7. 教师文档审核数据流

```text
GET /teacher/document.action
        ↓
Controller 查询教师课题下的 documents
        ↓
request.setAttribute("documents")
        ↓
forward teacher/documents.jsp
```

写操作：

```text
POST /teacher/document.action
        ↓
校验 score
        ↓
DocumentDao.review()
        ↓
WHERE document.status='submitted'
AND topic.teacher_id=当前教师
        ↓
更新 status/score/feedback/reviewer
        ↓
通知学生
        ↓
redirect GET 页面
```

## 8. 消息数据流

```mermaid
flowchart LR
    A[发送表单] --> B[MessageController]
    B --> C{canSendTo?}
    C -- 否 --> D[forbidden]
    C -- 是 --> E[MessageDao.insert]
    E --> F[(messages)]
    F --> G[收件箱 JSP]
```

查看消息时 DAO 使用：

```sql
WHERE id=?
AND (sender_id=? OR receiver_id=?)
```

避免用户通过修改消息 ID 查看其他人的消息。

## 9. 答辩安排数据流

```mermaid
flowchart TD
    A[管理员填写答辩安排] --> B[AdminDefenseController]
    B --> C[查询用户]
    C --> D{启用学生?}
    D -- 否 --> E[拒绝]
    D -- 是 --> F{存在 approved 选题?}
    F -- 否 --> E
    F -- 是 --> G{final 已 reviewed?}
    G -- 否 --> E
    G -- 是 --> H{分数 0-100?}
    H -- 否 --> E
    H -- 是 --> I[DefenseScheduleDao]
    I --> J[(defense_schedules)]
    J --> K[通知学生]
```

## 10. 统计 AJAX 数据流

```mermaid
sequenceDiagram
    participant J as statistics.jsp
    participant C as AdminStatsController
    participant D as StatsDao
    participant M as MySQL
    participant E as ECharts

    J->>C: fetch('/admin/stats.action')
    C->>D: selectionStats/docPassStats/scoreDistribution
    D->>M: COUNT/GROUP BY
    M-->>D: 聚合结果
    D-->>C: Map/List
    C-->>J: JSON
    J->>E: setOption(data)
    E-->>J: 图表
```

对应课程知识：

- JavaScript `fetch`
- Servlet 输出响应
- DAO 聚合查询
- JSON
- DOM/图表渲染

## 11. 页面跳转规则

### 查询请求

```text
GET action
  ↓
Controller 查询
  ↓
request 属性
  ↓
forward JSP
```

### 写请求

```text
POST action
  ↓
Controller 修改
  ↓
redirect GET action
  ↓
重新查询
  ↓
forward JSP
```

第二种叫 PRG：

```text
Post -> Redirect -> Get
```

优点：

- 刷新不会重复新增或审批。
- 地址栏是可再次访问的 GET URL。
- 页面显示的数据来自最新数据库状态。

## 12. 三类角色数据流边界

| 角色 | URL 前缀 | 允许的核心操作 |
| --- | --- | --- |
| 管理员 | `/admin/` | 用户、公告、答辩、统计、日志 |
| 教师 | `/teacher/` | 课题、选题审批、文档评阅 |
| 学生 | `/student/` | 浏览课题、申请、提交文档、查看成绩 |

`AuthFilter` 根据 URL 前缀和 Session 中的角色统一判断，避免只依赖页面是否显示链接。

## 13. 数据库主要关系

```mermaid
erDiagram
    USERS ||--o{ TOPICS : teacher
    USERS ||--o{ TOPIC_SELECTIONS : student
    TOPICS ||--o{ TOPIC_SELECTIONS : receives
    USERS ||--o{ DOCUMENTS : submits
    TOPICS ||--o{ DOCUMENTS : belongs_to
    DOCUMENTS ||--o{ DOCUMENT_VERSIONS : has
    USERS ||--o| DEFENSE_SCHEDULES : assigned
    USERS ||--o{ MESSAGES : sends
    USERS ||--o{ MESSAGES : receives
    USERS ||--o{ OPERATION_LOGS : operates
```

## 14. 从课程示例到当前项目的数据流升级

### classwork3

```text
JSP -> DAO -> JDBC -> MySQL
```

适合解释最基础的数据访问。

### classwork4/讲义第四、第五版

```text
Servlet -> DAO -> SQLHelper -> MySQL
       \-> JSP
```

引入 MVC 和 Controller。

### 当前项目

```text
Filter -> Controller -> DAO -> SQLHelper/Druid -> MySQL
                    \-> JSP
```

新增权限、连接池和事务，但主链路与课程完全一致。
