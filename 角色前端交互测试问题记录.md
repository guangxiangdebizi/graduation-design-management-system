# 角色前端交互测试问题记录

测试目标：围绕管理员、系主任、教师、学生四类用户，使用后端真实数据库数据登录前端页面，检查交互、数据显示、权限边界和空数据展示是否一致。

测试数据来源：

- `sql/seed_demo_data_20260622.sql`
- `sql/seed_showcase_data_20260623.sql`
- `sql/seed_scope_visual_data_20260623.sql`
- `sql/seed_role_interaction_test_data_20260623.sql`

## 测试账号矩阵

| 角色 | 账号 | 场景 |
|---|---|---|
| 管理员 | `admin` | 全量管理数据 |
| 管理员 | `admin_audit` | 新增审计管理员 |
| 管理员 | `admin_ops` | 新增运维管理员 |
| 系主任 | `director_ai_cs` | 计算机专业，有大量课题、选题、文档、答辩数据 |
| 系主任 | `director_ai_ai` | 人工智能专业，有完整专业统计数据 |
| 系主任 | `director_ee_power` | 电力专业，刻意准备为空数据/少数据范围 |
| 教师 | `teacher_test_cs` | 有课题、待审选题、待审文档 |
| 教师 | `teacher_test_ai` | 有人工智能专业完整流程数据 |
| 教师 | `teacher_empty_cs` | 无课题无关联数据 |
| 学生 | `student81` | 已通过选题，有开题文档、待审中期、答辩安排 |
| 学生 | `student82` | 选题待审核，无文档 |
| 学生 | `student83` | 未选题空状态 |

## 问题列表

> 后续测试发现的问题追加到这里，统一修复后再在“修复记录”标注。

### ISSUE-001 管理员统计页 500

- 发现方式：Playwright 角色矩阵测试。
- 复现账号：`admin`、`admin_audit`。
- 复现页面：`/admin/statistics.jsp`。
- 现象：页面返回 500，浏览器 console 出现 `Failed to load resource: the server responded with a status of 500`。
- 后端日志：`/WEB-INF/includes/sidebar.jsp` 编译失败，`loginUser cannot be resolved`。
- 初步原因：`WebContent/admin/statistics.jsp` 重写后未定义 `User loginUser = (User) session.getAttribute("loginUser")`，但 sidebar include 依赖该变量。
- 修复状态：已修复。`WebContent/admin/statistics.jsp` 已补充 `bean.User` 导入和 `loginUser` 会话变量定义；回归 `/admin/statistics.jsp` 返回 200，页面包含 `chartDefense`。

### ISSUE-002 学生仪表盘选题状态误判

- 发现方式：后端数据对照前端仪表盘。
- 复现账号：`student82`。
- 复现页面：`/dashboard.jsp`。
- 数据库事实：`student82` 在 `topic_selections` 中有 1 条 `pending` 选题申请，且无 `approved` 选题。
- 现象：仪表盘顶部“选题状态”卡片显示“未选题”，但同页“我的选题”表格能看到待审核记录。
- 初步原因：`WebContent/dashboard.jsp` 只用 `findApprovedByStudent()` 判断是否“已通过”，没有 approved 时一律显示“未选题”，忽略 pending/rejected 历史状态。
- 修复状态：已修复。学生仪表盘改为优先显示 approved；无 approved 时按最近一条申请显示“待审核”或“已驳回”；完全无申请才显示“未选题”。

### ISSUE-003 课题审核默认筛选为空时文案误导

- 发现方式：子代理只读审查 + 数据库范围对照。
- 复现账号：`director_ai_ai`、`admin`。
- 复现页面：`/director/topic-review.action`、`/admin/topic-review.action`。
- 数据库事实：部分范围内存在 `open/rejected` 课题，但默认页面只筛选 `pending`。
- 现象：默认筛选无待审课题时，页面提示“暂无本专业课题/暂无课题”，容易让人误解为后端没有任何课题。
- 初步原因：Controller 默认 `status=pending` 合理，但 JSP 空态文案没有说明“当前只是待审核筛选为空”。
- 修复状态：已修复。系主任课题审核页按当前状态筛选动态显示“暂无本专业待审核课题 / 暂无本专业已驳回课题”等空态文案；管理员课题审核职责已下线，后续不再提供 `/admin/topic-review.action`。

### ISSUE-004 消息联系人全量暴露且系主任可跨专业发消息

- 发现方式：子代理只读审查。
- 复现账号：`student83`、`teacher_test_cs`、`director_ai_cs`。
- 复现页面：`/student/messages.jsp`、`/teacher/messages.jsp`。
- 现象：发送消息弹窗使用全量用户列表。学生、教师、系主任都能看到很多实际不可发送或不属于本专业的对象；后端再用 `forbidden` 拒绝，前端和权限边界不一致。
- 初步原因：三个消息 JSP 都调用 `userDao.findAll(null)`；`MessageController.canSendTo()` 中 director 被 `RoleUtil.hasRole(...,"teacher")` 当教师处理，未按系主任专业范围收敛。
- 修复状态：已修复。新增 `MessageContactUtil` 统一联系人列表和后端发送校验：管理员全量；系主任仅管理员 + 本专业教师/学生；教师仅管理员 + 与自己课题有关的学生；学生仅管理员 + 已通过选题的指导教师。

### ISSUE-005 消息查看即已读后当前页列表仍可能显示未读

- 发现方式：子代理只读审查。
- 复现账号：`student90`。
- 复现页面：`/student/messages.jsp?view=<messageId>`。
- 数据库事实：`student90` 有未读消息 `交互测试消息动作待读`。
- 现象：JSP 先查询列表，再执行 `markRead()`；当前页详情已读，但列表行可能仍显示“新/未读”，刷新后才一致。
- 初步原因：消息 JSP 的已读状态更新顺序晚于列表查询。
- 修复状态：已修复。三个消息 JSP 在查询列表和未读数之前先处理 `view` 对应消息的已读更新，当前页列表和未读数保持一致。

### ISSUE-006 教师文档审核页缺少已退回筛选且空态文案固定

- 发现方式：子代理只读审查。
- 复现账号：`teacher_test_cs`。
- 复现页面：`/teacher/document.action?type=proposal&status=rejected`。
- 现象：后端支持 `rejected` 状态查询，审核动作也能产生退回文档，但 UI 只有待审核、已评阅、全部；不同筛选为空时都显示“暂无待审文档”。
- 初步原因：`WebContent/teacher/documents.jsp` 筛选按钮和空态文案写死。
- 修复状态：已修复。教师文档审核页补充“已退回”筛选，并按 `submitted/reviewed/rejected/all` 动态显示空态文案。

### ISSUE-007 管理员存在课题审核权限，角色职责越界

- 发现方式：业务逻辑复核。
- 复现账号：`admin`。
- 复现页面：`/dashboard.jsp`、`/admin/topic-review.action`、管理员侧边栏。
- 现象：管理员仪表盘和侧边栏存在“课题审核”入口，后端也有 `/admin/topic-review.action` 可审批全部课题。
- 权限模型：管理员负责系统管理、用户、公告、系统开关、模板、答辩安排、全局统计等平台级事务；课题是否通过应由对应专业的系主任管理。
- 初步原因：早期把“课题审核”作为全局管理员功能实现，和后续“系主任管理本专业项目”的分层模型冲突。
- 修复状态：已修复。删除管理员课题审核 Servlet 和 JSP，移除管理员侧边栏/仪表盘入口；保留 `/director/topic-review.action` 作为本专业课题审核唯一入口。

### ISSUE-008 姓名字段混入身份/职称，用户表展示模型不清

- 发现方式：业务逻辑复核 + 真实数据库抽查。
- 复现账号：`director01`、`director_ai_ai`、`teacher01`、`teacher02`、`admin`。
- 复现页面：`/admin/user.action`、`/profile.action`、顶部用户信息、站内消息联系人。
- 数据库事实：`users.role` 已能表达权限角色，但 `real_name` 中存在“人工智能学部系主任 / 计算机科学与技术系主任 / 张教授 / 李副教授 / 系统管理员”等混合值。
- 现象：表格里“姓名”列实际显示身份或岗位，导致“系主任/教师/管理员”看起来像用户姓名，不符合用户实体建模。
- 权限模型：`role` 表示系统权限角色；`real_name` 只表示真人姓名；`title` 表示身份/职称/岗位，如学生、教授、副教授、讲师、系主任、管理员。
- 修复状态：已修复。新增 `users.title` 字段和迁移脚本，清洗演示账号姓名；用户管理页按“姓名、ID、用户名、学院、专业、身份/职称、权限角色、班级、学号”展示；个人中心、顶部用户信息、消息联系人改用身份/职称展示。

## 回归测试记录

### 第一轮角色矩阵测试

- 覆盖账号：`admin`、`admin_audit`、`director_ai_cs`、`director_ai_ai`、`director_ee_power`、`teacher_test_cs`、`teacher_empty_cs`、`student81`、`student82`、`student83`。
- 覆盖页面：仪表盘、管理员用户/系统开关/公告/答辩/统计/模板/消息/日志，系主任教师继承页面、本专业审核/统计，教师课题/选题/文档/学生/答辩/模板/消息，学生选题/文档/成绩/答辩/模板/消息。
- 发现问题：`ISSUE-001`。

### 第二轮回归测试

- 覆盖账号：`admin`、`admin_audit`、`director_ee_power`、`teacher_empty_cs`、`student83`。
- 重点场景：管理员统计页、空数据系主任、空数据教师、未选题学生、系主任访问管理员系统开关。
- 结果：所有被测页面返回 200；`director_ee_power` 访问 `/admin/system-switch.action` 被重定向回 `dashboard.jsp`；无 console error；空数据页面显示“暂无/未选题/未安排”等空状态文案。

### 数据库一致性抽检

- `director_ee_power` 范围：`topics=0`、`active_selections=0`、`docs=0`、`defenses=0`，前端显示本专业空数据，符合预期。
- `teacher_empty_cs`：`topics=0`、`selections=0`、`docs=0`、`defenses=0`，前端显示“还没有发布课题 / 暂无选题申请 / 暂无待审文档 / 暂无答辩安排”，符合预期。
- `student81`：`selections=1`、`approved=1`、`docs=2`、`defenses=1`，前端显示已通过选题、开题已审核、中期已提交、答辩已安排，符合预期。
- `student82`：`selections=1`、`approved=0`、`docs=0`、`defenses=0`，前端显示待审核选题且禁止文档提交，符合预期。
- `student83`：`selections=0`、`approved=0`、`docs=0`、`defenses=0`，前端显示未选题、无成绩、无答辩安排，符合预期。
- 活跃跨专业选题抽检：查询无返回行，说明 `pending/approved` 选题未跨专业。

### 第三轮真实交互动作回归

- 覆盖账号：`student82`、`student84`、`student91`、`student88`、`student89`、`student90`、`student83`、`teacher_test_cs`、`director_ai_cs`、`director_ai_ai`、`director_ee_power`、`admin`。
- 学生仪表盘状态：
  - `student82`：数据库为 `pending`，仪表盘选题状态显示“待审核”。
  - `student84`：数据库为 `rejected`，仪表盘选题状态显示“已驳回”。
- 学生申请选题：
  - `student91` 通过 `/student/topic.action` 申请 `交互测试：计算机专业待审选题课题`，数据库新增 `pending` 选题申请，申请原因为 `action-test-apply`。
- 教师审批选题：
  - `teacher_test_cs` 通过 `/teacher/selection.action` 批准 `student88` 的待审申请，数据库状态变为 `approved`，并给 `student88` 生成 `选题审批结果` 通知消息。
- 教师审核文档：
  - `teacher_test_cs` 通过 `/teacher/document.action` 审核 `student89` 开题报告，数据库状态变为 `reviewed`、分数 `88.50`，并给 `student89` 生成 `文档审核结果` 通知消息。
- 消息模块：
  - `student90` 打开未读消息详情后，数据库 `is_read=1`，当前页面对应行不再显示“新/未读”。
  - `student90` 可给自己的指导教师 `teacher_test_cs` 发送消息，数据库新增 1 条发送记录。
  - `student83` 无已通过选题，联系人列表不显示 `teacher_test_cs`；强行 POST 给该教师，数据库新增数为 0。
  - `director_ai_cs` 联系人列表不显示人工智能专业学生 `student85`；强行跨专业 POST，数据库新增数为 0。
- 模板下载：
  - `student91` 下载模板 `id=1` 返回 HTTP 200，文件大小 176 字节，并带 `Content-Disposition` 附件头。
- 权限边界和范围隔离：
  - `director_ai_cs` 访问 `/admin/system-switch.action` 返回 302 并跳回 `/dashboard.jsp`。
  - `director_ai_cs` 搜索 `人工智能专业完整流程课题` 不显示跨专业表格行；`director_ai_ai` 同搜索能显示该课题。
  - `director_ee_power` 的 `/director/stats.action` 返回：选题已选题 0、待审批 0、文档通过数全 0、答辩已评分 0、待评分 0、成绩分布为空数组。
- 页面编译/访问：
  - `/director/topic-review.action`、`/teacher/document.action?type=proposal&status=rejected`、`/teacher/messages.jsp`、`/admin/messages.jsp`、`/student/messages.jsp` 均返回 200。
